Done:
    Apache:
    - Bygger en image med Wordpress och ldap-account-manager.
    - Skapar en wordpress.conf fil och enablear den.
    WP:
    - Genererar och kopplar till MySQL automatiskt via start.sh
    - Installerar (men inte aktiverar) 3 ldap plugins automatiskt.
    MySQL:
    - Officiell Docker image.
    - Skapar en wordpress databas, användare och lösen via miljövariabler.
    OpenLDAP:
    - Bygger en image med slapd.

    Docker-compose:
    - Lägger alla containrar på samma nätverk. Länkar containers via containernamn.
    - Mappar portar 80, 389, 636 och 3306. 
        (Egentligen borde kanske inte 389, 636 och 3306 mappas utåt, kommunikation sker ju inom nätverket.)
    - Tar miljövariabler från en .env-fil
    - Skapar namngedda volymer för viktiga mappar.

Todo: 

Rensa miljövariabler efter start. Start-scriptet kan skapa en fil med ett värde som den läser varje boot, om värdet inte finns så vet scriptet att det är första booten och köra igenom första-boot-grejer.

Automatiserad installation av slapd får inget lösenord, så man kan inte logga in på admin-kontot för config.
Samma sak för dpkg-reconfigure slapd, måste skriva in domän och adminlösen manuellt.

Känsliga variabler i .env stannar som variabler i containers. Och i start.sh i /
Kommentera scripts.
Schemalägg build-script.

Lägg in docker-entrypoint-init.b i imagen.
Automatisera installationen av ldap wp-plugin.
Importera användare till WP via script -> LDAP -> WP.
SSH-server med nyckelautentisering på host.
Själv-signerat SSL cert till Apache. 
Apache server config

Backup-script på host.
 - Backa upp vad? /var/lib/mysql

Containers kör som root...
IP-tables för att droppa trafik som inte kommer mellan containers.
Fail2Ban på apache, ssh, mysql och openldap.
Let's encrypt