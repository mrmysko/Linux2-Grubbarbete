Vilka konton finns de atm?

Mysql containerns root.

Wordpress databasens admin.
Default Wordpress admin.

LDAP config admin. (cn=admin,dc=config)
LDAP directory manager. (cn=admin,dc=<domän>)

LAM har ett master password för att konfigurera sig själv.
Och varje Server Profile som innehåller konfiguration för olika LDAP-servrar har sitt eget lösen.
Kopplas LAM ihop med MySQL behövs en databas och ett konto med lösen där också.